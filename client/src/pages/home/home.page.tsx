import { Center } from "@chakra-ui/layout";

export const HomePage = () => {
  return (
    <Center height="100vh" fontSize="3xl">
      CRA using Urql with Fastify using Apollo GraphQL
    </Center>
  );
};
